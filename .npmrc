auto-install-peers=true
strict-peer-dependencies=false
# Jangan auto-install/verify sebelum tiap script (hindari error build-script
# opsional yang sengaja diabaikan). Deps di-install eksplisit via `pnpm install`.
verify-deps-before-run=false
